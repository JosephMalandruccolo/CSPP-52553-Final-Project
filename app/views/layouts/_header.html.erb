<div class="header">
	<%= image_tag("collegelens.png", alt:'collegelens') %>
	
	<% if !User.find_by_id(session[:user_id]).nil? %>
		<%= User.find_by_id(session[:user_id]).name %>
		<%= link_to "Sign out", signout_path %>
	<% else %>
		<%= link_to "Sign in", signin_path %>
	<% end %>
	
	
</div>